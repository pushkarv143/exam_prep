package com.examprep.enrollment.entity;

/** How the student got access. Useful for revenue reports and support queries. */
public enum EnrollmentSource {
    FREE,
    PAYMENT,
    ADMIN,
    BATCH
}
