package com.examprep.question.model;

/** Input format of a NUMERICAL answer. The student keypad follows it. */
public enum NumericFormat {
    /** Whole numbers only, e.g. JEE Main "integer type". */
    INTEGER,
    /** Decimals allowed. */
    DECIMAL
}
