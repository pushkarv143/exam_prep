package com.examprep.question.workflow;

/** Things a staff member can do with a question; the studio shows only the allowed ones. */
public enum QuestionAction {
    EDIT,
    SUBMIT,
    CLAIM,
    ASSIGN,
    REQUEST_CHANGES,
    APPROVE,
    PUBLISH,
    ARCHIVE,
    RESTORE,
    COMMENT
}
