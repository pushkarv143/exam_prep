package com.examprep.question.entity;

/** What the question tests (a simplified Bloom's taxonomy). */
public enum CognitiveLevel {
    /** Remember a fact or formula. */
    RECALL,
    /** Apply a concept to a standard situation. */
    APPLY,
    /** Combine concepts or reason through an unfamiliar situation. */
    ANALYSE
}
