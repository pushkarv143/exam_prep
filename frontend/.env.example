# Base path of the REST API as seen by the browser. Keep the default relative path: in dev
# Vite proxies it, and in Docker nginx does.
VITE_API_BASE_URL=/api/v1

# Where the Vite dev server proxies /api (only used by `npm run dev`).
VITE_PROXY_TARGET=http://localhost:8080
